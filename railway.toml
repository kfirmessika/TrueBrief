[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=/app/src uvicorn truebrief.api.server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
